name: Workflow Commands
on: [push]

jobs:
    testing-wf-command:
        runs-on: ubuntu-latest
        steps:
            - name: setting an error message
              run: echo "::error::Misssing semicolen"
            - name: Setting an error message with parms
              run: echo"::error titlr:Error titlr,file=app.js::"